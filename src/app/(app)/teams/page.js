import React from "react";
import { Teams } from "@/components/import/import";
const page = () => {
  return (
    <div className="bg-white mt-0">
      <Teams />
    </div>
  );
};

export default page;
