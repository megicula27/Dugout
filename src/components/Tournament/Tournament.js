import React from "react";
import { TournamentFilter } from "../import/import";
const Tournament = () => {
  return (
    <div>
      <TournamentFilter />
    </div>
  );
};

export default Tournament;
